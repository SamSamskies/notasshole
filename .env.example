# Server-only. Never prefix with VITE_ (that would ship the key to the browser).
# Used by the Vercel /api fallback locally (`vercel dev`) and in production env.
GEMINI_API_KEY=
# Optional. Defaults to gemini-2.5-flash on the server.
# GEMINI_MODEL=gemini-2.5-flash
